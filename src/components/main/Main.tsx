import type { FunctionComponent } from "react";
import { MainText } from "./Main.styled";

const Main: FunctionComponent = () => {
  return (
    <MainText>
      We design lean internal platforms that replace Excel chaos and scattered
      tools. Built with no-code, low-code or lightweight dev, our systems are
      fast to deploy and easy to evolve. For ambitious teams who want to move
      faster, with less effort.
    </MainText>
  );
};

export default Main;
